using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using HoloToolkit.Unity.InputModule;
using HoloToolkit.Unity.SpatialMapping;
public class StartMenuManager : MonoBehaviour {
    /*public void OnInputClicked(InputClickedEventData eventData)
    {
        //gameObject.GetComponent<HandDraggable>().IsDraggingEnabled = false;
        //gameObject.transform.GetChild(0).gameObject.SetActive(true);
        //gameObject.transform.GetChild(1).gameObject.SetActive(true);
    }*/

    private void Start()
    {
        //GameObject menu = Instantiate(gameObject, Vector3.zero, Quaternion.identity);
        //RaycastHit hit;
        //if(Physics.Raycast(Camera.main.transform.position, Camera.main.transform.forward, out hit, 30.0f, SpatialMappingManager.Instance.LayerMask))
        //{
        //    transform.position = hit.point;
        //}
        //transform.position = Camera.main.transform.position + Camera.main.transform.forward * 2.0f;
    }  
}
